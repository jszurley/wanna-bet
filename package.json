{
  "name": "wanna-bet",
  "version": "1.0.0",
  "description": "Wanna Bet? - Friendly betting app",
  "scripts": {
    "postinstall": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
